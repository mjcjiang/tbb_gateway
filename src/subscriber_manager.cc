#include "subscriber_manager.h"

int SubscriberManager::add_user(const std::string& user_name) {
    return 0;
}

int SubscriberManager::delete_user(const std::string& user_name) {
    return 0;
}

int get_push_address(const std::string& user_name, std::string& push_address) {
    return 0;
}
